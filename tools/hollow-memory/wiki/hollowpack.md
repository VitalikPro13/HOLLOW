# .hollowpack — the artist shop's art container

Built 2026-08-29 for the artist shop (design record: `reports/ARTIST_SHOP_DESIGN.md`; memory `project_hollowpack_format`, `project_artist_shop_design`). Phase 1 = the store sells DRM-free art; phase 2 binds a blind-signed support credential to the art's hash.

## Format (v1)

A ZIP: `pack.json` + `files/<sha256>.webp`. Manifest fields: `format: "hollowpack"`, `version: 1`, `item {id, title, kinds}`, `artist {name, slug, url}`, `license`, `files [{role, path, sha256, bytes, w, h, animated}]`, `ext {}` (reserved for the phase-2 issuing key and catalog signature). Roles (ceilings per the 2026-09-01 encoder bump, memory `project_encoder_ceilings_bump`): `frame` (square, at most 512, animated or still -- a CEILING since the bump, so older exactly-128 packs stay valid), `avatar` (still-only, 512 ceiling), `avatar_anim` + `avatar_still` (the pair `process_user_avatar_anim` returns), `banner` (still-only, 2.5:1, 1200x480 ceiling), `banner_anim` + `banner_still`. Animated vs still is decided from BYTES (`is_animated_image`), never the extension. `item.id` defaults to the first 16 hex of sha256 over the sorted file hashes (content-addressed).

**Identity is the hash of the PROCESSED bytes.** The pack is produced only by the app's own encoders (`process_avatar_frame`, `process_avatar_image`, `process_user_avatar_anim`, `process_banner_image`, `process_user_banner_anim`) so a pack made by the tool and art the app processes itself land on the same hash (libwebp is deterministic). A lossy WebP re-encoded is a different hash, so **the importer never re-encodes**.

## Code

- `rust/hollow_core/src/hollowpack.rs`: format structs (`#[serde(default)]` on every optional field, unknown fields ignored, `format`/`version` checked), `process_source`, `processed_from_bytes` (passthrough for bundles: verify, hash, never re-encode), `build_pack`, `catalog_entry`, `read_manifest`, `verify_pack` / `verify_pack_file` (the ONE trust boundary; see `security_write_gates.md` §2).
- `rust/hollow_art/` (since 2026-09-02): the crate the CLI is built from. It has NO code of its own: `src/lib.rs` and `src/node/mod.rs` `#[path]`-include `hollow_core/src/hollowpack.rs`, `node/image_convert.rs` and `node/webp_anim.rs`, so the tool compiles the app's exact encoder sources without dragging MLS/SQLCipher/WebRTC along, and its `Cargo.lock` was seeded from hollow_core's so `image`/`image-webp`/`webp-animation`/`libwebp-sys2`/`zip` resolve to the versions the app ships (re-seed it when hollow_core bumps one). The included files' `#[cfg(test)]` modules reach hollow_core-only modules, so tests run in hollow_core (`cargo nextest run --lib hollowpack image_convert`), never in hollow_art. The old `packtool` feature and `[[bin]]` in hollow_core are gone.
- `rust/hollow_art/src/main.rs`: the CLI, `pack` / `inspect` / `process` / `version`. Build: `cd rust/hollow_art && cargo build --release` (Windows, ~25 s, `target/release/hollowpack.exe`; a copy lives at `anonlisten-sites/shop/.data/bin/hollowpack.exe` for the shop's local dev server). **Shop host build:** `pip install ziglang cargo-zigbuild`, `rustup target add x86_64-unknown-linux-musl`, then `cargo zigbuild --release --target x86_64-unknown-linux-musl` (zig 0.16 + cargo-zigbuild 0.23 verified; static musl, 2.7 MB, runs on the host's glibc-2.28 CloudLinux). Installed at `~/domains/anonlisten.com/shop/data/bin/hollowpack` on Hostinger (outside `hbuilds`, so a deploy never removes it); a 1 MB JPEG encodes in ~0.3 s there. `pack` flags: `--emit-entry <json>` (the catalog entry the shop ingests), `--emit-files <dir>` (every processed file as `<dir>/<sha256>.webp`), and the buyer's download `--delivery <zip>` = the pack at the root (named after `--out`) + each `--original <path>` under `originals/<basename>` (basename only, duplicates refused, STORED) + `--readme <file>` as `README.txt` (deflated). **Bundles (CLI 0.2.0, 2026-09-02):** `--processed <role>=<path>` (repeatable, mixable with `--file`, and `pack` needs at least one of the two) includes an ALREADY-processed WebP as it is: `hollowpack::processed_from_bytes` checks it against the role's ceiling, its animation expectation and the see-through-centre gate for a frame, hashes it, and never re-encodes it, because a second lossy pass would mint a different hash and the buyer of a bundle has to own the same hash as the buyer of the single item. `<role>` is the WIRE role (`frame`, `avatar`, `avatar_anim`, `avatar_still`, `banner`, `banner_anim`, `banner_still`); a `--processed avatar_anim` without its `avatar_still` in the same invocation is refused, likewise the banner pair, and `build_pack` refuses two files of one role at AUTHORING only (`verify_pack` is untouched, so packs already in the world keep verifying). The version went 0.1.0 to 0.2.0 for this: the shop shows the encoder version on its status card and requires 0.2.0 before it will build a bundle. Exit 0 on success, 1 with `hollowpack: <reason>` on stderr otherwise; `pack` prints one `encoded ...` line per encoded file, one `included ...` line per `--processed` file, and one `wrote ...` line per artifact on stdout.
- FFI `import_hollowpack(path) -> HollowpackImport` and `list_owned_art()` in `api/network.rs`: verify, then store bytes AS-IS on the asset rail (kind `frame` for frames, kind `profile` for every avatar/banner role, stills included) and record `owned_art(hash, role, item_id, title, artist_name, artist_slug, artist_url, license, imported_at)` in `storage/messages.rs`. Importing does NOT wear anything; "wear it" is a later step that reads the still back off the rail and sets the profile the way the pickers do.
- `image_convert::validate_frame_centre` re-applies the see-through-centre gate to already-encoded frame bytes.

## Verification (import and `inspect` are the same call)

Caps first (8 files, 4 MB each, 16 MB total, 20 MB zip, 64 KB manifest; the biggest legal single asset is a 2 MB animated avatar or banner), then per file: recomputed sha256 must match, the WebP must decode, dimensions within the role's ceiling (frame: square and at most 512), animated roles animate and still roles do not, frames pass the centre gate. A manifest that disagrees with the bytes about size, dims or animation is REFUSED, never silently corrected. Nothing is written by a manifest-supplied name. 17 unit tests (`cargo nextest run --lib hollowpack`).

## The shop runs it (server-side approval, 2026-09-02)

The keeper no longer runs the CLI by hand. In `anonlisten-sites/shop/`, an artist marks every submitted file with a ROLE (`avatar`, `banner`, `frame`, or `source` = ships only; `src/lib/submission_roles.js`, validated by `checkSubmissionRoles` with magic-byte sniffing), and **Approve and list** on `/admin/ingest` does the whole act in `src/lib/server/approve.js`: `packtool.js` execFile's the binary at `config.hollowpackBin` (`HOLLOWPACK_BIN`, default `<SHOP_DATA_DIR>/bin/hollowpack`, 90 s timeout under the host's 120 s cap) with `pack --file <role>=<path> ... --emit-entry --emit-files` (the `--delivery` zip exists in the CLI but the shop no longer builds one: the originals go on the Creem product as they are, downloaded one by one from the listing page), feeds the emitted entry + WebPs through the SAME `ingestSubmission` a hand-made pack takes (hash recompute, row `submitted` to `approved` in one transaction), records `listings.pack_file` (migration 6, relative `packs/<id>/<stem>.hollowpack`; `delivery_file` stays NULL now), then `createProductFor` (Creem `POST /v1/products`: onetime, USD, tax exclusive, digital-goods-service, `image_url` = the shop's `/art/<hash>` when the origin is https). The SPLIT is NOT part of the button (Vitalik, 2026-09-02): `createSplitFor` runs from the listing page's own button once the product carries license keys + files, because creating the split is what emails the artist the invite (store id if known, else the artist's sealed Creem address `artists.creem_email_enc`, else their sign-in address; cleared by `setArtistStore`). What the Creem API cannot set, license keys and the delivery file, is a checklist on `/admin/listing/<id>` read back off `GET /v1/products?product_id=` (`features[].type` = `licenseKey` / `file`). The manual door (entry JSON + WebPs) stays at the bottom of the submissions page. Never encode in JavaScript (sharp) for a listing: identity is the app encoder's bytes.

**Bundles of existing listings (2026-09-02, dashboards round 2).** A bundle is a NEW listing made of two or more of one artist's `approved`/`live` listings (migration 7, `listing_parts(listing_id, part_listing_id)`; `db.submitBundle` writes the row exactly like `submitListing` plus an EMPTY `listing_submissions` row so every reader keeps working; `partsForListing` is the one "is this a bundle" question). The rules live in client-safe `src/lib/bundle.js` (`checkBundleParts`: 2+ parts, each approved or live, kinds pairwise DISJOINT because a role is a slot, one wearable per role) and are re-asked at approval; `bundle_form.js` reads both forms with the artist id as an ARGUMENT (the cookie on `/artist/bundles`, a picker behind the admin cookie on `/admin/bundles`), refuses a part that is itself a set, and `existingBundleOf` refuses the same set twice. Approval takes the same button and the same `buildListingFromSubmission`, which branches on parts: the parts' processed files go to the binary as `--processed <role>=<dataDir>/art/<sha256>` (hash shape re-checked before the join) and `files: []`, so the bundle sells the SAME hashes the single pieces sell; nothing is re-encoded. It needs `hollowpack` 0.2.0 (`bundleToolRefusal`, `versionAtLeast`), installed on the host 2026-09-02 with 0.1.0 kept beside it. The keeper's page runs `approveAndList` at once (nothing to review); the artist's lands a `submitted` row the keeper approves on Submissions, where the parts are shown instead of a file list. A set cannot go inside another set. The submit form separately asks whether an UPLOAD is one item or a bundle (`checkSubmissionShape` in `submission_roles.js`: one wearable, or two or more); both doors land as 2+ kinds and the catalog shows them under /bundles alike.

**Pricing, sales and the split, after Vitalik's review (2026-09-02, later).** Titles are at least 3 characters and descriptions at least 10 and REQUIRED, on the submit form and both bundle doors (`MIN_TITLE`/`MIN_DESCRIPTION` in `bundle.js`, one sentence each, shown as the field hint and the refusal). The artist reprices and discounts their OWN listings from `/artist/listings` (`PriceControls.svelte`, actions `setPrice`/`startSale`/`endSale`, tenancy from the cookie, floor $4.99, a sale is under the list price for 1 to 30 days): Creem is told FIRST (`rail.updateProduct` = measured `PATCH /v1/products/<id>` with `{price}`; the dashboard-only license-key and file features survive a repricing) and nothing changes locally on a refusal; every change lands as a CLOSED `listing_requests` row so the keeper's page keeps the history, and "Ask for a change" is for anything else. Sales live in `listings.sale_cents`/`sale_until` (migration 8) and the EFFECTIVE price is computed at read time (`sale.js`): the catalog's `price_cents`/`price` are the effective price (so the app's Shop tab shows a sale with no app change) plus `was`/`was_cents`/`sale_until` while a sale is on; the storefront strikes `was` beside the price. Expired sales are put back at Creem then cleared by `endExpiredSales` (cron, artist listings load, admin listing load; a refusing rail leaves the row for the next pass). **The split at approval:** once an artist has accepted ONE invite their `creem_store_id` is known, and a split to a store answers enabled with nobody to accept, so `approveAndList` now creates that split right after the product (`splitAtApproval`, store recipient only, never an address); an artist without a store keeps the listing-page button. Because of that, and because a quick invite acceptance could always have done it, AUTO-publish (cron + page-load reconciliation, `splitCheckPlan(listings, splits, checklists)`) now also waits for the Creem product to carry license keys AND a file (`creemChecklist` via `rail.getProduct` per candidate; a failed read is "not yet"); the keeper's manual Publish keeps its product+split gate. The reconciliation cron (`woVqdomfD7`, replaced `SzYCuzd5IP`) runs hourly at :20.

**Item addresses are the listing's SLUG, never a file hash (2026-09-02, evening).** A bundle carries the same frame hash as the single frame by design, so `/item/<sha256>` was ambiguous and opened the set (Vitalik hit it live). `/item/<slug>` is the address (`params/itemslug.js` refuses a bare 64-hex so the two matchers stay disjoint); `/item/<hash>` is a 301 through `item_address.js` `listingForFileHash` (drawn-by-that-hash first, then fewest kinds, then oldest); the catalog emits `slug` + absolute `url`; the app's `api/shop.rs` builds `item_url` from the slug. The item view shows only the item's own slots (`fitting.svelte.js` `soloLook`); a set's wall card is the profile composite (`ProfileBlock.svelte`, shared with the dashboards' `PiecePreview`).

## The app client (built 2026-09-02)

The buyer's side of the pack, in the app. Design record `reports/ARTIST_SHOP_DESIGN.md` §14 item 2; memory `project_shop_app_client`.

- **Store-build gate, decided ONCE at startup** (`lib/src/core/shop_availability.dart`, primed in `main()` before `runApp`): desktop = shop; iOS and web = none; Android = none when the installer package is a store (`com.android.vending`, Amazon, Samsung, read through the `getInstallerPackage` method-channel call in `MainActivity.kt`), full shop for a sideload; the `--dart-define=HOLLOW_STORE_BUILD=true` define forces it off anywhere. Widgets read `shopAvailableProvider`, never the static. On a store build there is NO shop surface at all (design §8: "the safest sentence is no sentence"): no button, no dashboard, no import panel, no redeem dialog, and a `hollow://redeem` link reads as unrecognised. Rendering of bought art is untouched everywhere.
- **The Shop is a centre tab** (`ShellTab.shop`, `shopTabOpenProvider` in `shop_tab_provider.dart`, opened by `openShopTab(read)` which mirrors `ConferenceNotifier.openTab`): the store icon sits BEFORE Saved messages on the Dock friends strip and after Conferences on the Classic rail; lit = open, pressing the lit button closes it (issue #28 rule). Mobile reaches it from Settings ("Hollow Shop" tile after Help, `ShopDashboard(embedded: true)`).
- **Rust `api/shop.rs`** (read-only against a PINNED origin, `SHOP_ORIGIN`; a debug build honours `HOLLOW_SHOP_ORIGIN` for the local dev shop, release builds ignore it): `fetch_shop_catalog` (GET `/api/catalog.json` on `get_http_runtime`, 15 s, 4 MB cap checked on Content-Length AND while streaming, no redirects, tolerant per-field/per-element serde so a newer shop column narrows a listing rather than blanking the wall; `display_hash`/`still_hash`/`primary_kind`/`bundle`/`wide` are a faithful port of the shop's `catalog.js` rules, unit-tested against them) and `fetch_shop_art(hash)` (GET `/art/<hash>`, 4 MB cap, the bytes are REFUSED unless their SHA-256 equals the requested hash and they carry a RIFF/WEBP header, and they are NEVER stored: preview art is not owned art). Dart caches previews in RAM only (`shopArtProvider`, 48 entries).
- **Dashboard** (`lib/src/ui/shop/shop_dashboard.dart`): filter chips All/Frames/Avatars/Banners/Bundles, a grid of cards (a frame card draws YOUR avatar wearing it, an avatar card your face replaced, a banner its strip; "Owned" chip when any file hash is in `ownedHashesProvider`), "Import a pack", "Open in browser", refresh, and a `.hollowpack` drop zone on desktop. A card opens `shop_item_dialog.dart`: bigger preview, description, artist link (`/@slug`), licence, price, and **Buy = `launchUrl(item_url)` in the system browser** (never an in-app purchase), or **Wear it** when owned.
- **Import** (`hollowpack_import.dart`): file picker (custom `.hollowpack` filter on desktop, `FileType.any` on mobile) or drop; `import_hollowpack` then an "Imported" dialog listing the files with per-kind Wear buttons (+ "Wear all" for a bundle). Failures show Rust's message verbatim.
- **Owned art + the wear step** (`owned_art_provider.dart`): `list_owned_art` grouped by `item_id` into `OwnedItem` (role getters: frame, avatar anim/still, banner anim/still). `OwnedArtNotifier.wear(item, kinds)` reads the still and the animation OFF THE RAIL (`getEmoteBytes`, kind-agnostic), then ONE `updateMyProfile` carrying the current text fields plus the worn media: frame = the hash; avatar/banner = the still as the pushed blob and the anim hash on the rail (a still-only piece sends `''` for the anim so an older animation stops playing over it); other fields null = preserve. Optimistic: `avatarProvider.setAvatar` + `ProfileNotifier.patchMyMedia`; the banner cache converges on the `ProfileUpdated` event, never a read-back. "Worn" = `myWornHashesProvider` (frame id, anim hashes, and the sha256 of my avatar/banner still bytes memoised per instance). **Owned hashes are PINNED in the evictor's keep-set** (`referenced_asset_hashes` unions `owned_art`; test `owned_art_is_pinned_against_asset_eviction`).
- **"Art you own" panel** (`owned_art_panel.dart`): last section of the desktop Profile settings and after the Avatar frame button on mobile; import button + drop zone, one row per item with thumbnail, artist link, kind chips and Wear/Worn buttons; below it "Codes kept for later".
- **`hollow://redeem/<code>` (phase-2 preparation):** `HollowLinkType.redeem` (`^[A-Za-z0-9_-]{8,128}$`, percent-decoded via `pathSegments`), a chat card ("Support code", tap to keep) and the deep-link arm both call `showRedeemCodeDialog`, which KEEPS the code (`keep_redeem_code`: shape check, 64-row cap, repeat = no-op; table `redeem_codes`) and explains that the supporter mark arrives in a later update. Nothing is redeemed yet: phase 2 owns the blind signing and MUST honour the M4 gate (`isKeyRefused` + `isKeyBurned` before `activateKey`; `burnKey` + `incrementRedeemed` as the authority) on the shop side.
- **Tests:** Rust `cargo nextest run --lib shop owned_art redeem` (catalog tolerance, display rule port, art hash refusal, code shape and cap, eviction pin) plus the `#[ignore]` `live_catalog_smoke`; Dart `test/shell_tab_test.dart`, `test/hollow_link_utils_test.dart`, `test/widget/friends_bar_toggle_test.dart`, `test/widget/owned_art_panel_test.dart`, `test/widget/shop_dashboard_test.dart`; the probe scenario `scripts/probe_scenarios/shop_tab.json` (needs `PACK_PATH` in the environment; new op `import_pack`).

## Not yet built

Phase 2: the redeem itself (`node/support_creds.rs`, blind signing, the `support_creds` profile field and the marks). The shop server lives in the private `anonlisten-sites` repo (`shop/`).
