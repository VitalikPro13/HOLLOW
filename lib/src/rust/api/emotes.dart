// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.11.1.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These functions are ignored because they are not marked as `pub`: `ffz_query`, `normalize_name`, `parse_ffz_rows`

/// Wire token for an emote, as inserted into message text / reactions.
String emoteToken({required String name, required String hash}) =>
    RustLib.instance.api.crateApiEmotesEmoteToken(name: name, hash: hash);

/// Process a user-picked image (PNG/JPG/WebP/GIF) into an emote blob and
/// cache it locally. Caller then registers it via [add_personal_emote] or
/// [add_server_emote].
Future<ProcessedEmote> processAndStoreEmote({required List<int> rawBytes}) =>
    RustLib.instance.api.crateApiEmotesProcessAndStoreEmote(rawBytes: rawBytes);

/// Cached emote bytes for a hash (None = not cached yet; caller should
/// trigger [request_emotes] and re-read on `EmoteAssetsReceived`).
Future<Uint8List?> getEmoteBytes({required String hash}) =>
    RustLib.instance.api.crateApiEmotesGetEmoteBytes(hash: hash);

/// Pull emote bytes we don't have from the network: `server_id` asks one
/// online member of that server's room, `peer_hint` asks a DM sender's
/// devices. Fire-and-forget; results arrive as `EmoteAssetsReceived`.
Future<void> requestEmotes({
  required List<String> hashes,
  String? serverId,
  String? peerHint,
}) => RustLib.instance.api.crateApiEmotesRequestEmotes(
  hashes: hashes,
  serverId: serverId,
  peerHint: peerHint,
);

Future<void> addPersonalEmote({
  required String name,
  required String hash,
  required bool animated,
  required String source,
}) => RustLib.instance.api.crateApiEmotesAddPersonalEmote(
  name: name,
  hash: hash,
  animated: animated,
  source: source,
);

Future<void> removePersonalEmote({required String name}) =>
    RustLib.instance.api.crateApiEmotesRemovePersonalEmote(name: name);

Future<List<PersonalEmote>> listPersonalEmotes() =>
    RustLib.instance.api.crateApiEmotesListPersonalEmotes();

/// The custom emote set of a server (replicated metadata; bytes pulled
/// separately by hash).
Future<List<ServerEmote>> getServerEmotes({required String serverId}) =>
    RustLib.instance.api.crateApiEmotesGetServerEmotes(serverId: serverId);

/// Add/replace a custom server emote (MANAGE_EMOTES, Admin+ by default).
/// The blob must already be cached locally (via [process_and_store_emote]
/// or [ffz_import_emote]) so this node can serve members' pull requests.
Future<void> addServerEmote({
  required String serverId,
  required String name,
  required String hash,
  required bool animated,
}) => RustLib.instance.api.crateApiEmotesAddServerEmote(
  serverId: serverId,
  name: name,
  hash: hash,
  animated: animated,
);

Future<void> removeServerEmote({
  required String serverId,
  required String name,
}) => RustLib.instance.api.crateApiEmotesRemoveServerEmote(
  serverId: serverId,
  name: name,
);

/// Search FFZ emotes via the website's write-through cache. Authoring only.
Future<List<FfzEmote>> ffzSearch({required String query}) =>
    RustLib.instance.api.crateApiEmotesFfzSearch(query: query);

/// The FFZ global emote sets (cached server-side; effectively static).
Future<List<FfzEmote>> ffzGlobal() =>
    RustLib.instance.api.crateApiEmotesFfzGlobal();

/// Hand-curated popular emotes — the FFZ tab's default (empty-search) view.
/// The list itself lives server-side in search.php; an older proxy without
/// the mode answers `[]`, and the caller falls back to [ffz_global].
Future<List<FfzEmote>> ffzCurated() =>
    RustLib.instance.api.crateApiEmotesFfzCurated();

/// Download an FFZ emote image FROM OUR CDN (authoring-time only), process
/// it into a Hollow emote blob, and cache it. The allowlist is the privacy
/// boundary — this can never be abused as a generic fetcher.
Future<ProcessedEmote> ffzImportEmote({required String imageUrl}) =>
    RustLib.instance.api.crateApiEmotesFfzImportEmote(imageUrl: imageUrl);

/// One FFZ search/browse row (authoring-time only). `image_url` points at
/// OUR CDN cache, never at FFZ.
class FfzEmote {
  final PlatformInt64 id;
  final String name;
  final String owner;
  final bool animated;
  final String imageUrl;
  final PlatformInt64 usageCount;

  const FfzEmote({
    required this.id,
    required this.name,
    required this.owner,
    required this.animated,
    required this.imageUrl,
    required this.usageCount,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      name.hashCode ^
      owner.hashCode ^
      animated.hashCode ^
      imageUrl.hashCode ^
      usageCount.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is FfzEmote &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          name == other.name &&
          owner == other.owner &&
          animated == other.animated &&
          imageUrl == other.imageUrl &&
          usageCount == other.usageCount;
}

/// One entry of the user's personal (global) emote set.
class PersonalEmote {
  final String name;
  final String hash;
  final bool animated;

  /// "upload" | "ffz:<id>" — provenance only, never fetched at display time.
  final String source;

  const PersonalEmote({
    required this.name,
    required this.hash,
    required this.animated,
    required this.source,
  });

  @override
  int get hashCode =>
      name.hashCode ^ hash.hashCode ^ animated.hashCode ^ source.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is PersonalEmote &&
          runtimeType == other.runtimeType &&
          name == other.name &&
          hash == other.hash &&
          animated == other.animated &&
          source == other.source;
}

/// A processed, locally cached emote blob (content-addressed).
class ProcessedEmote {
  final String hash;
  final bool animated;

  const ProcessedEmote({required this.hash, required this.animated});

  @override
  int get hashCode => hash.hashCode ^ animated.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ProcessedEmote &&
          runtimeType == other.runtimeType &&
          hash == other.hash &&
          animated == other.animated;
}

/// One custom emote of a server's emote set (CRDT-replicated metadata).
class ServerEmote {
  final String name;
  final String hash;
  final bool animated;

  const ServerEmote({
    required this.name,
    required this.hash,
    required this.animated,
  });

  @override
  int get hashCode => name.hashCode ^ hash.hashCode ^ animated.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ServerEmote &&
          runtimeType == other.runtimeType &&
          name == other.name &&
          hash == other.hash &&
          animated == other.animated;
}
