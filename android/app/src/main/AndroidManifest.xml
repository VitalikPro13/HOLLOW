<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!-- App lock: fingerprint / face unlock (local_auth). -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- allowAudioPlaybackCapture=false: Hollow's OWN playback (call voices,
         received screen-share audio) is never re-captured by AudioPlaybackCapture,
         killing the echo loop when this phone shares its screen with audio.
         The uid-exclude API can't be mixed with usage matching, so this manifest
         flag is the anti-echo mechanism (also blocks third-party recorders from
         taping Hollow calls — a privacy win). -->
    <!-- No OS backups: the data dir holds identity.key (plaintext unless the
         opt-in at-rest protection is on), the SQLCipher store and the
         keystore-wrapped app-lock secret. Google Auto Backup would ship all of
         it to Drive, and a device-to-device transfer would clone this DEVICE
         identity onto a second phone, which multi-device forbids. The .hollow
         backup is the only sanctioned copy. allowBackup covers cloud backup on
         every API level; fullBackupContent covers API 30 and below;
         dataExtractionRules covers API 31+ including device transfer. -->
    <application
        android:label="Hollow"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:allowAudioPlaybackCapture="false">
        <!-- Portrait-only (incl. tablets): landscape/tablet layouts are the
             mobile shell's deferred edge cases; also locked at runtime in
             main.dart. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:screenOrientation="portrait"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- hollow:// deep links (server invites, shares, recovery links).
                 launchMode=singleTop delivers repeat links via onNewIntent,
                 which app_links surfaces on uriLinkStream. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="hollow"/>
            </intent-filter>
        </activity>
        <provider
            android:name="com.superlist.super_native_extensions.DataProvider"
            android:authorities="com.anonlisten.hollow.SuperClipboardDataProvider"
            android:exported="true"
            android:grantUriPermissions="true" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
