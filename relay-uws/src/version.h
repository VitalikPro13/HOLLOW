#pragma once

// Reported by /relay-status so a client can tell what a relay it was pointed
// at supports before it connects. Bump on a wire- or feature-visible change.
#define HOLLOW_RELAY_VERSION "0.11.1"
